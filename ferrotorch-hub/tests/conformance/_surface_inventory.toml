# Surface inventory for ferrotorch-hub.
#
# Auto-maintained by this conformance suite. Every `pub` item that appears
# on ferrotorch-hub's API surface lives here as a [[item]] entry with its
# module path, kind, and a one-line description. New items added to
# `ferrotorch-hub/src/` must appear here (or in `_surface_exclusions.toml`)
# before the coverage gate passes.
#
# Reference: huggingface_hub == 0.24.3
# Generated by: scripts/regenerate_hub_fixtures.py

# ---------------------------------------------------------------------------
# cache.rs — local cache management
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::cache::HubCache"
kind = "struct"
description = "Local file-system cache for pretrained model weights."

[[item]]
path = "ferrotorch_hub::cache::HubCache::new"
kind = "method"
description = "Create a cache backed by the given directory."

[[item]]
path = "ferrotorch_hub::cache::HubCache::with_default_dir"
kind = "method"
description = "Create a cache using the default directory (~/.ferrotorch/hub/)."

[[item]]
path = "ferrotorch_hub::cache::HubCache::cache_dir"
kind = "method"
description = "Return the directory backing this cache."

[[item]]
path = "ferrotorch_hub::cache::HubCache::has"
kind = "method"
description = "Check if weights for the given model name are already cached."

[[item]]
path = "ferrotorch_hub::cache::HubCache::path"
kind = "method"
description = "Return the cache file path for a model name."

[[item]]
path = "ferrotorch_hub::cache::HubCache::path_for_model"
kind = "method"
description = "Return the cache file path for a ModelInfo, with the correct extension."

[[item]]
path = "ferrotorch_hub::cache::HubCache::store"
kind = "method"
description = "Store raw bytes in the cache under the given name."

[[item]]
path = "ferrotorch_hub::cache::HubCache::load"
kind = "method"
description = "Load cached weights as raw bytes."

[[item]]
path = "ferrotorch_hub::cache::HubCache::clear"
kind = "method"
description = "Remove all files from the cache directory."

[[item]]
path = "ferrotorch_hub::cache::default_cache_dir"
kind = "fn"
description = "Return the default cache directory: ~/.ferrotorch/hub/."

# ---------------------------------------------------------------------------
# registry.rs — static pretrained model registry
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::registry::WeightsFormat"
kind = "enum"
description = "Format of the serialized weights file (SafeTensors or FerrotorchStateDict)."

[[item]]
path = "ferrotorch_hub::registry::ModelInfo"
kind = "struct"
description = "Metadata for a pretrained model (name, URL, SHA-256, format, param count)."

[[item]]
path = "ferrotorch_hub::registry::list_models"
kind = "fn"
description = "List all available pretrained models in the static registry."

[[item]]
path = "ferrotorch_hub::registry::get_model_info"
kind = "fn"
description = "Get info for a specific model by name from the static registry."

# ---------------------------------------------------------------------------
# download.rs — weight download and load
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::download::download_weights"
kind = "fn"
description = "Resolve the cached weights file for a model, downloading if needed."

[[item]]
path = "ferrotorch_hub::download::load_pretrained"
kind = "fn"
description = "Load a pretrained model's state dict by name."

[[item]]
path = "ferrotorch_hub::download::sanitize_path_component"
kind = "fn"
description = "Validate that a string is safe to use as a single path component (pub(crate))."

[[item]]
path = "ferrotorch_hub::download::hf_download_model"
kind = "fn"
description = "Download every safetensors shard from a HuggingFace model repo (http feature)."

# ---------------------------------------------------------------------------
# hf_config.rs — HuggingFace transformer config.json parser
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig"
kind = "struct"
description = "Parsed contents of a HuggingFace transformer config.json."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::from_json_str"
kind = "method"
description = "Parse HfTransformerConfig from a JSON string."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::from_file"
kind = "method"
description = "Parse HfTransformerConfig from a config.json file on disk."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::num_key_value_heads"
kind = "method"
description = "Number of key/value heads, with MHA default applied when absent."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::head_dim"
kind = "method"
description = "Per-head embedding dimension: hidden_size / num_attention_heads."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::is_gqa"
kind = "method"
description = "Whether the config describes a GQA model (kv_heads < q_heads)."

[[item]]
path = "ferrotorch_hub::hf_config::HfTransformerConfig::validate"
kind = "method"
description = "Validate structural invariants (divisibility, positive counts, supported activation)."

# ---------------------------------------------------------------------------
# auth.rs — HuggingFace Hub authentication helpers (http feature)
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::auth::hf_token"
kind = "fn"
description = "Read the HuggingFace auth token from HF_TOKEN, HF_HOME, or ~/.cache/huggingface/token."

[[item]]
path = "ferrotorch_hub::auth::with_auth"
kind = "fn"
description = "Decorate a ureq::Request with Authorization: Bearer when a token is available."

# ---------------------------------------------------------------------------
# discovery.rs — dynamic model discovery via HuggingFace Hub API (http feature)
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::discovery::HfModelSummary"
kind = "struct"
description = "A summary of a model returned by the HuggingFace Hub search API."

[[item]]
path = "ferrotorch_hub::discovery::HfModelInfo"
kind = "struct"
description = "Detailed info for a single model, returned by GET /api/models/{repo_id}."

[[item]]
path = "ferrotorch_hub::discovery::HfRepoFile"
kind = "struct"
description = "A single file entry in a HuggingFace model repo."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery"
kind = "struct"
description = "Parameters for a model search query against the HuggingFace Hub."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::new"
kind = "method"
description = "Create an empty query (no filters)."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::with_search"
kind = "method"
description = "Set the free-text search term."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::with_pipeline_tag"
kind = "method"
description = "Filter to a specific pipeline tag."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::with_library"
kind = "method"
description = "Filter to a specific library."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::with_limit"
kind = "method"
description = "Limit the number of results returned."

[[item]]
path = "ferrotorch_hub::discovery::SearchQuery::with_sort"
kind = "method"
description = "Set the sort key."

[[item]]
path = "ferrotorch_hub::discovery::search_models"
kind = "fn"
description = "Search for models on the HuggingFace Hub."

[[item]]
path = "ferrotorch_hub::discovery::get_model"
kind = "fn"
description = "Fetch detailed info for a single model by its repo id."

# ---------------------------------------------------------------------------
# lib.rs — top-level re-exports
# ---------------------------------------------------------------------------

[[item]]
path = "ferrotorch_hub::HubCache"
kind = "re-export"
description = "pub use cache::HubCache"

[[item]]
path = "ferrotorch_hub::default_cache_dir"
kind = "re-export"
description = "pub use cache::default_cache_dir"

[[item]]
path = "ferrotorch_hub::HfModelInfo"
kind = "re-export"
description = "pub use discovery::HfModelInfo (http feature)"

[[item]]
path = "ferrotorch_hub::HfModelSummary"
kind = "re-export"
description = "pub use discovery::HfModelSummary (http feature)"

[[item]]
path = "ferrotorch_hub::HfRepoFile"
kind = "re-export"
description = "pub use discovery::HfRepoFile (http feature)"

[[item]]
path = "ferrotorch_hub::SearchQuery"
kind = "re-export"
description = "pub use discovery::SearchQuery (http feature)"

[[item]]
path = "ferrotorch_hub::get_model"
kind = "re-export"
description = "pub use discovery::get_model (http feature)"

[[item]]
path = "ferrotorch_hub::search_models"
kind = "re-export"
description = "pub use discovery::search_models (http feature)"

[[item]]
path = "ferrotorch_hub::hf_token"
kind = "re-export"
description = "pub use auth::hf_token (http feature)"

[[item]]
path = "ferrotorch_hub::with_auth"
kind = "re-export"
description = "pub use auth::with_auth (http feature)"

[[item]]
path = "ferrotorch_hub::hf_download_model"
kind = "re-export"
description = "pub use download::hf_download_model (http feature)"

[[item]]
path = "ferrotorch_hub::download_weights"
kind = "re-export"
description = "pub use download::download_weights"

[[item]]
path = "ferrotorch_hub::load_pretrained"
kind = "re-export"
description = "pub use download::load_pretrained"

[[item]]
path = "ferrotorch_hub::HfTransformerConfig"
kind = "re-export"
description = "pub use hf_config::HfTransformerConfig"

[[item]]
path = "ferrotorch_hub::ModelInfo"
kind = "re-export"
description = "pub use registry::ModelInfo"

[[item]]
path = "ferrotorch_hub::WeightsFormat"
kind = "re-export"
description = "pub use registry::WeightsFormat"

[[item]]
path = "ferrotorch_hub::get_model_info"
kind = "re-export"
description = "pub use registry::get_model_info"

[[item]]
path = "ferrotorch_hub::list_models"
kind = "re-export"
description = "pub use registry::list_models"
