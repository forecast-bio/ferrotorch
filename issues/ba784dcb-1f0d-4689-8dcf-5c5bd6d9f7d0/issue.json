{
  "uuid": "ba784dcb-1f0d-4689-8dcf-5c5bd6d9f7d0",
  "display_id": 1138,
  "title": "Pass 5.B.4 follow-up: wire FusedChain PTX runtime executor (jit ↔ gpu integration; route apply_fused on CUDA)",
  "description": "Spun off from #1106 (Pass 5.B.4). Pre-flight reassessed the audit's 'silently downgrades' claim: `Tensor::data()` (ferrotorch-core/src/tensor.rs:635-638) already Errs with `GpuTensorNotAccessible` on CUDA inputs, so `apply_fused` is NOT a silent demote — it fails-fast already. #1106 cleaned up the doc drift and replaced the generic `GpuTensorNotAccessible` with an explicit `NotImplementedOnCuda` guard.\n\nThis follow-up tracks the audit's *Pattern B* path (deferred): route CUDA-input `apply_fused` through `FusedChain::generate_ptx` + a real GPU runtime executor.\n\n## Why this was deferred from #1106\n\nWiring a runtime PTX executor requires workspace-level `ferrotorch-jit` ↔ `ferrotorch-gpu` integration that's structurally infeasible in a leaf-crate audit-fix:\n\n1. ferrotorch-jit's `Cargo.toml` has optional `cudarc` but does NOT depend on `ferrotorch-gpu` — adding that dep is workspace-level architectural work, not a leaf-crate fix.\n2. The existing PTX-loader pattern lives at `ferrotorch-gpu/src/module_cache.rs:74` (`ctx.load_module(Ptx::from_src(ptx_src))` + `module.load_function(kernel_name)`). Reusing it from `ferrotorch-jit` requires either adding ferrotorch-gpu as a dep, factoring out a shared 'jit-runtime' crate, or moving the dispatch into ferrotorch-gpu and inverting the dep direction.\n3. `codegen.rs:987-1017` already correctly returns `JitError::GpuBackendUnavailable` for `GpuCuda`/`GpuPtx` targets — the same blocker.\n\n## Scope estimate\n\n- ~500 LoC across ferrotorch-jit + ferrotorch-gpu (or new shared crate)\n- New dep + test infra (CUDA-having CI runner, or cfg-gated tests with hardware probe)\n- Workspace-level decision on the dep direction (jit→gpu vs. gpu→jit vs. shared crate)\n\n## Acceptance for this follow-up\n\n- `apply_fused` on a CUDA tensor dispatches through `FusedChain::generate_ptx` and the wired runtime executor, returning a CUDA tensor (matching device, no CPU readback).\n- Discriminating test on a CUDA-having runner: input `is_cuda()` ⇒ output `is_cuda()`.\n- ferrotorch-jit `compile()` for `GpuCuda`/`GpuPtx` targets honors the target instead of returning `GpuBackendUnavailable`.\n\n## References\n\n- #1106 — fail-fast doc/error cleanup (this follow-up's parent leaf-fix)\n- #982 — umbrella audit\n- ferrotorch-gpu/src/module_cache.rs:74 — existing PTX loader pattern\n- ferrotorch-jit/src/fusion.rs:260 — `FusedChain::generate_ptx` (codegen, no runtime)\n- ferrotorch-jit/src/codegen.rs:987-1017 — STALE-NO-OP CUDA target arm",
  "status": "closed",
  "priority": "medium",
  "parent_uuid": "60feedba-482e-4c56-bb7a-236ea5a99b17",
  "created_by": "y4rr",
  "created_at": "2026-05-10T18:46:14.088057262Z",
  "updated_at": "2026-05-10T19:47:57.936649686Z",
  "closed_at": "2026-05-10T19:47:57.936649686Z",
  "labels": [
    "audit,gpu-cpu-mismatch,jit,refactor"
  ]
}